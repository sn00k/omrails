<% if user_signed_in? %>
	<% # Put anything here when user is signed in %>
<% else %>
		<div class="hero-unit">
		<h1>Welcome!</h1>
			<p>
				Test Application.
			</p>
			<p>
				<%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
				
			</p>
</div>
<% end %>